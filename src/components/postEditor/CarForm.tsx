import React from "react";

export type CarState = {
  postId: number;
  kilometrage: number;
  year: number;
  transmission: string;
  brand: string;
  model: string;
  color: string;
  engine: string;
  damaged: boolean;
};

const CarForm = () => {
  return <>CarForm</>;
};

export default CarForm;
